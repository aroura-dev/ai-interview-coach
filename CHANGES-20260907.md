# MirrorAgent 功能增量说明（2026-09-07）

> 本文件记录本次会话在 MirrorAgent 上完成的功能开发与验证结果。
> 所有改动均为**本地未提交**状态；对外口径请先解决仓库归属问题再决定是否提交/推送。

## 1. 检索评测体系（离线可复现）
- `EvalMetrics.java`：新增 `calcNDCGAtK`。
- `OfflineBm25BaselineTest`：离线 BM25 回归（无需 DashScope/Milvus），`data/eval` 语料+黄金集，
  输出 `data/eval/reports/offline_bm25_baseline.md`，门槛：词面子集 Recall@10≥0.50 / MRR≥0.90 / nDCG@10≥0.55。
- `dataset_v1.json`：50 → 86 条，新增 36 条语义改写难例（`mode:"semantic"`，仅线上评测用，BM25 路不计门槛）。
- `.github/workflows/ci.yml`：后端 `mvn test` CI（含离线回归）。
- 关键结论：旧 50 条中 84% 为种子题直配（MRR=1.0 失真）；语义难例 BM25-only MRR=0.30，为线上向量+重排提供对比基线。

## 2. 历史会话（去掉"开发中"占位）
- 后端：`interview_records` 增 `report_md/review_plan_md` 列（自动 ALTER）；`MySQLStore` 读方法；
  `HistoryController`：`GET /api/history`、`GET /api/history/{sessionId}`（Bearer JWT）。
- 前端：`HistoryPage` + `Sidebar` 导航；`/api/history.ts`。

## 3. 学习统计
- `ProfileController`：`GET /api/profile/summary`（次数/平均/最高/进步差/历次序列/薄弱点/技能/快照）。
- 前端：`StatsPage`（指标卡 + 历次得分条 + 薄弱点趋势 + 技能标签）。

## 4. 画像历史快照 + 逐主题趋势
- 新表 `profile_snapshots`（每场面试结束自动快照：薄弱点数/平均分/`weak_points_json`）。
- 前端 `StatsPage`：薄弱点数量趋势柱 + 逐主题"首次→最近"变化表。

## 5. SM-2 间隔复习
- 纯算法 `Sm2Scheduler`（含单测 `Sm2SchedulerTest` 4 例）；新表 `review_items`。
- 面试评分自动同步：得分 <80 建/保复习项，≥80 置 mastered 出队。
- `ReviewController`：`GET /api/review/today`、`GET /api/review/items`、`POST /api/review/grade`。
- 前端：`ReviewPage`（今日待复习打分：忘了/困难/轻松 → SM-2 排期）+ 侧栏"今日复习"。

## 验证状态
- 后端 `mvn compile` ✅；测试 13+4 全绿 ✅；前端 `tsc --noEmit` 0 错误 ✅；Vite 编译各页面 200 ✅。
- 接口实测：history/profile/review 401/404/聚合/分级均正确；演示数据已清理（review_items/snapshots/records=0）。

## 运行方式
- 基础设施：`docker compose -p mirroragent -f MirrorAgent-java/docker-compose.yml -f MirrorAgent-java/docker-compose.override.yml up -d`
- 后端：`MirrorAgent-java/start-backend.ps1`（日志 backend-run.log，端口 9090）
- 前端：`MirrorAgent-web/start-frontend.ps1`（默认 5174；5173 被 Sentinel 占用）

## 本地环境注意事项（未提交）
- `docker-compose.override.yml`：MySQL 3308 + MinIO 宿主端口改 19000/19001（9001 被 Windows 拒绝绑定）。
- `start-backend.ps1` / `start-frontend.ps1`：本地启动辅助脚本。